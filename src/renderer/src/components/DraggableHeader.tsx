

export const DraggableHeader = ({ onMouseEnter, onMouseLeave }) => {

    return (
        <header onMouseEnter={onMouseEnter} onMouseLeave={onMouseLeave} >
            <header >test?</header>
        </header>
    )
}